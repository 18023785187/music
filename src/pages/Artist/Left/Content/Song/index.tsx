/**
 * 歌手歌曲
 */
import React, { memo } from 'react'

interface IProps {
    id: string
}

function Song(props: IProps) {
    //  const { id } = props

    return (
        <div></div>
    )
}

export default memo(Song)
